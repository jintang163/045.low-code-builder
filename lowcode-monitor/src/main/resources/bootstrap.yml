spring:
  application:
    name: lowcode-monitor
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: lowcode
      config:
        server-addr: 127.0.0.1:8848
        namespace: lowcode
        file-extension: yml

server:
  port: 8088

logging:
  level:
    com.lowcode.monitor: debug
